<Window x:Class="Facturon.App.Views.Dialogs.NewTaxRateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Tax Rate"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        WindowStyle="ToolWindow"
        FocusManager.FocusedElement="{Binding ElementName=CancelButton}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Code:" Grid.Row="0" Grid.Column="0" Margin="0,0,5,0" />
        <TextBox x:Name="CodeBox" Grid.Row="0" Grid.Column="1"
                 Text="{Binding Rate.Code, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,2,0,0" Foreground="Red"
                   Text="{Binding ElementName=CodeBox, Path=(Validation.Errors)[0].ErrorContent}" />

        <TextBlock Text="Value (%):" Grid.Row="1" Grid.Column="0" Margin="0,5,5,0" />
        <TextBox x:Name="ValueBox" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"
                 Text="{Binding Rate.Value, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,2,0,0" Foreground="Red"
                   Text="{Binding ElementName=ValueBox, Path=(Validation.Errors)[0].ErrorContent}" />

        <TextBlock Text="Valid From:" Grid.Row="2" Grid.Column="0" Margin="0,5,5,0" />
        <DatePicker Grid.Row="2" Grid.Column="1" Margin="0,5,0,0" SelectedDate="{Binding Rate.ValidFrom}" />

        <TextBlock Text="Valid To:" Grid.Row="3" Grid.Column="0" Margin="0,5,5,0" />
        <DatePicker x:Name="ValidToPicker" Grid.Row="3" Grid.Column="1" Margin="0,5,0,0" SelectedDate="{Binding ValidTo, ValidatesOnDataErrors=True}" />
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,2,0,0" Foreground="Red"
                   Text="{Binding ElementName=ValidToPicker, Path=(Validation.Errors)[0].ErrorContent}" />

        <CheckBox Content="Active" Grid.Row="4" Grid.Column="1" Margin="0,5,0,0" IsChecked="{Binding IsActive}" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="75" Margin="0,0,5,0" IsDefault="True" Command="{Binding SaveCommand}" />
            <Button x:Name="CancelButton" Content="Cancel" Width="75" IsCancel="True" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>
