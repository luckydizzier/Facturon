<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="WindowBackground" Color="White" />

    <!-- Validation template used across dialogs -->
    <ControlTemplate x:Key="DefaultValidationTemplate">
        <DockPanel LastChildFill="True">
            <Border BorderBrush="Red" BorderThickness="1">
                <AdornedElementPlaceholder />
            </Border>
            <TextBlock DockPanel.Dock="Right" Margin="5,0,0,0"
                       Foreground="Red"
                       Text="{Binding [0].ErrorContent}" />
        </DockPanel>
    </ControlTemplate>

    <!-- Apply validation styling to all text boxes -->
    <Style TargetType="TextBox">
        <Setter Property="Validation.ErrorTemplate"
                Value="{StaticResource DefaultValidationTemplate}" />
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={RelativeSource Self},
                                        Path=(Validation.Errors)[0].ErrorContent}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Default DataGrid row style with alternating row colors -->
    <SolidColorBrush x:Key="RowHighlight" Color="#FFE6F7FF" />
    <!-- Apply alternating row colors -->
    <Style TargetType="DataGrid">
        <Setter Property="ItemsControl.AlternationCount" Value="2" />
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="White" />
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="#FFF7F7F7" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource RowHighlight}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
